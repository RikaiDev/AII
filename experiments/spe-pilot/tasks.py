"""Task ladder for the SPE pilot.

Each task is a contract: a function signature + docstring (the prompt side)
and a frozen list of assert-based tests (the kernel side). The tests are
SHOWN in the prompt — SPE defines acceptance before work, visibly — but the
model can only submit a function body; it never touches the test text that
the kernel executes. Tiers: t1 trivial (GPT-2-class models should sometimes
succeed), t2 easy, t3 medium.
"""

TASKS = [
    # --- t1: trivial -------------------------------------------------------
    dict(id="add", tier=1, sig="def add(a, b):",
         doc="Return the sum of a and b.",
         tests=["assert add(1, 2) == 3", "assert add(-1, 1) == 0",
                "assert add(0, 0) == 0"]),
    dict(id="double", tier=1, sig="def double(x):",
         doc="Return x multiplied by 2.",
         tests=["assert double(2) == 4", "assert double(-3) == -6",
                "assert double(0) == 0"]),
    dict(id="is_even", tier=1, sig="def is_even(n):",
         doc="Return True if n is even, else False.",
         tests=["assert is_even(2) is True", "assert is_even(3) is False",
                "assert is_even(0) is True"]),
    dict(id="maximum", tier=1, sig="def maximum(a, b):",
         doc="Return the larger of a and b.",
         tests=["assert maximum(1, 2) == 2", "assert maximum(5, 3) == 5",
                "assert maximum(-1, -2) == -1"]),
    dict(id="square", tier=1, sig="def square(x):",
         doc="Return x squared.",
         tests=["assert square(3) == 9", "assert square(-2) == 4",
                "assert square(0) == 0"]),
    dict(id="greet", tier=1, sig="def greet(name):",
         doc="Return the string 'Hello, ' followed by name and '!'.",
         tests=["assert greet('Ann') == 'Hello, Ann!'",
                "assert greet('Bo') == 'Hello, Bo!'"]),
    dict(id="negate", tier=1, sig="def negate(x):",
         doc="Return the negation of x.",
         tests=["assert negate(5) == -5", "assert negate(-3) == 3",
                "assert negate(0) == 0"]),
    dict(id="first_char", tier=1, sig="def first_char(s):",
         doc="Return the first character of the non-empty string s.",
         tests=["assert first_char('abc') == 'a'",
                "assert first_char('z') == 'z'"]),
    dict(id="length", tier=1, sig="def length(items):",
         doc="Return the number of elements in the list items.",
         tests=["assert length([1, 2, 3]) == 3", "assert length([]) == 0"]),
    dict(id="is_empty", tier=1, sig="def is_empty(s):",
         doc="Return True if the string s is empty, else False.",
         tests=["assert is_empty('') is True",
                "assert is_empty('a') is False"]),
    dict(id="last_item", tier=1, sig="def last_item(items):",
         doc="Return the last element of the non-empty list items.",
         tests=["assert last_item([1, 2, 3]) == 3",
                "assert last_item(['x']) == 'x'"]),
    dict(id="concat", tier=1, sig="def concat(a, b):",
         doc="Return the two strings a and b joined together.",
         tests=["assert concat('ab', 'cd') == 'abcd'",
                "assert concat('', 'x') == 'x'"]),
    dict(id="half", tier=1, sig="def half(x):",
         doc="Return x divided by 2 as a float.",
         tests=["assert half(4) == 2.0", "assert half(3) == 1.5"]),
    dict(id="to_upper", tier=1, sig="def to_upper(s):",
         doc="Return the string s converted to uppercase.",
         tests=["assert to_upper('abc') == 'ABC'",
                "assert to_upper('Hi') == 'HI'"]),
    dict(id="contains", tier=1, sig="def contains(items, x):",
         doc="Return True if x is in the list items, else False.",
         tests=["assert contains([1, 2], 2) is True",
                "assert contains([1, 2], 3) is False"]),

    # --- t2: easy ----------------------------------------------------------
    dict(id="reverse_string", tier=2, sig="def reverse_string(s):",
         doc="Return the string s reversed.",
         tests=["assert reverse_string('abc') == 'cba'",
                "assert reverse_string('') == ''",
                "assert reverse_string('ab') == 'ba'"]),
    dict(id="count_vowels", tier=2, sig="def count_vowels(s):",
         doc="Return the number of vowels (a, e, i, o, u, lowercase only) in s.",
         tests=["assert count_vowels('abc') == 1",
                "assert count_vowels('aeiou') == 5",
                "assert count_vowels('xyz') == 0"]),
    dict(id="sum_list", tier=2, sig="def sum_list(nums):",
         doc="Return the sum of all numbers in the list nums; 0 for an empty list.",
         tests=["assert sum_list([1, 2, 3]) == 6", "assert sum_list([]) == 0",
                "assert sum_list([-1, 1]) == 0"]),
    dict(id="factorial", tier=2, sig="def factorial(n):",
         doc="Return n! for a non-negative integer n. factorial(0) is 1.",
         tests=["assert factorial(0) == 1", "assert factorial(1) == 1",
                "assert factorial(5) == 120"]),
    dict(id="is_palindrome", tier=2, sig="def is_palindrome(s):",
         doc="Return True if the string s reads the same forwards and backwards.",
         tests=["assert is_palindrome('aba') is True",
                "assert is_palindrome('ab') is False",
                "assert is_palindrome('') is True"]),
    dict(id="sum_evens", tier=2, sig="def sum_evens(nums):",
         doc="Return the sum of the even numbers in the list nums.",
         tests=["assert sum_evens([1, 2, 3, 4]) == 6",
                "assert sum_evens([1, 3]) == 0",
                "assert sum_evens([]) == 0"]),
    dict(id="clamp", tier=2, sig="def clamp(x, lo, hi):",
         doc="Return x limited to the inclusive range [lo, hi].",
         tests=["assert clamp(5, 0, 10) == 5", "assert clamp(-1, 0, 10) == 0",
                "assert clamp(11, 0, 10) == 10"]),
    dict(id="count_words", tier=2, sig="def count_words(s):",
         doc="Return the number of whitespace-separated words in s.",
         tests=["assert count_words('a b c') == 3",
                "assert count_words('') == 0",
                "assert count_words('hello') == 1"]),
    dict(id="min_index", tier=2, sig="def min_index(nums):",
         doc="Return the index of the smallest number in the non-empty list nums.",
         tests=["assert min_index([3, 1, 2]) == 1",
                "assert min_index([5]) == 0",
                "assert min_index([2, 2, 1]) == 2"]),
    dict(id="repeat_each", tier=2, sig="def repeat_each(s):",
         doc="Return s with every character doubled, e.g. 'ab' -> 'aabb'.",
         tests=["assert repeat_each('ab') == 'aabb'",
                "assert repeat_each('') == ''",
                "assert repeat_each('x') == 'xx'"]),
    dict(id="fizzbuzz_one", tier=2, sig="def fizzbuzz_one(n):",
         doc=("Return 'FizzBuzz' if n is divisible by 15, 'Fizz' if by 3, "
              "'Buzz' if by 5, else the number n itself."),
         tests=["assert fizzbuzz_one(15) == 'FizzBuzz'",
                "assert fizzbuzz_one(9) == 'Fizz'",
                "assert fizzbuzz_one(10) == 'Buzz'",
                "assert fizzbuzz_one(7) == 7"]),
    dict(id="unique_keep_order", tier=2, sig="def unique_keep_order(items):",
         doc="Return a list of the items with duplicates removed, keeping first-seen order.",
         tests=["assert unique_keep_order([1, 2, 1, 3]) == [1, 2, 3]",
                "assert unique_keep_order([]) == []",
                "assert unique_keep_order(['a', 'a']) == ['a']"]),
    dict(id="swap_case_digits", tier=2, sig="def count_digits(s):",
         doc="Return how many characters of the string s are decimal digits.",
         tests=["assert count_digits('a1b2') == 2",
                "assert count_digits('abc') == 0",
                "assert count_digits('123') == 3"]),
    dict(id="second_largest", tier=2, sig="def second_largest(nums):",
         doc="Return the second-largest distinct value in nums (assume it exists).",
         tests=["assert second_largest([1, 3, 2]) == 2",
                "assert second_largest([5, 5, 4]) == 4",
                "assert second_largest([1, 2]) == 1"]),

    # --- t3: medium --------------------------------------------------------
    dict(id="balanced", tier=3, sig="def balanced(s):",
         doc=("Return True if the parentheses '(' and ')' in s are balanced "
              "and properly nested, else False. Other characters may appear."),
         tests=["assert balanced('(a)') is True",
                "assert balanced('(()') is False",
                "assert balanced(')(') is False",
                "assert balanced('') is True"]),
    dict(id="rle", tier=3, sig="def rle(s):",
         doc=("Run-length encode s: 'aaab' -> 'a3b1'. Empty string encodes "
              "to ''."),
         tests=["assert rle('aaab') == 'a3b1'", "assert rle('') == ''",
                "assert rle('abc') == 'a1b1c1'"]),
    dict(id="roman_small", tier=3, sig="def roman_small(n):",
         doc=("Return the Roman numeral for an integer n between 1 and 10 "
              "inclusive (I, II, III, IV, V, VI, VII, VIII, IX, X)."),
         tests=["assert roman_small(4) == 'IV'", "assert roman_small(9) == 'IX'",
                "assert roman_small(10) == 'X'", "assert roman_small(1) == 'I'"]),
    dict(id="merge_sorted", tier=3, sig="def merge_sorted(a, b):",
         doc="Merge two ascending-sorted lists into one ascending-sorted list.",
         tests=["assert merge_sorted([1, 3], [2, 4]) == [1, 2, 3, 4]",
                "assert merge_sorted([], [1]) == [1]",
                "assert merge_sorted([1, 1], [1]) == [1, 1, 1]"]),
    dict(id="most_common_char", tier=3, sig="def most_common_char(s):",
         doc=("Return the character occurring most often in the non-empty "
              "string s; on ties return the one whose first occurrence is "
              "earliest."),
         tests=["assert most_common_char('aab') == 'a'",
                "assert most_common_char('abb') == 'b'",
                "assert most_common_char('ba') == 'b'"]),
]
